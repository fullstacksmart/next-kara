import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  SectionItem: {
    display: 'flex',
    flexDirection: 'row',
  },
  ButtonContainer: {},
  ContentContainer: {
    overflow: 'hidden',
    flex: 1,
  },
});

export default useStyles;
